{% extends "base.html.twig" %}

{% block body %}
    {% for message in app.flashes('info') %}
        {{ message }}
    {% endfor %}
    <ul>
        {% for item in items %}
            <li>{{ item }}</li>

        {% endfor %}
    </ul>
{% endblock %}